# GOAL（今の1目標）

> 同時に1件だけ。終わったら次の1件に書き換える。

- **目標**: （例: CI の failing test `xxx` を直す / ガチャ画面の空状態を直す）
- **対象範囲**: （例: `lib/...`, `test/...` のみ）
- **やらないこと**: （例: リファクタ、依存更新、無関係な整形）
- **完了の定義**: `VERIFY.md` をすべて PASS
- **更新日**: YYYY-MM-DD
