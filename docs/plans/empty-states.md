# Plan: F3 空状態・エラー状態UX
Created: 2026-08-31
Status: IN_PROGRESS

## 要件
`docs/phase5_brushup_spec.md` §2-3 / §3-4。共通コンポーネント `EmptyStateCard` を新設し、対象画面の空状態・エラー状態が「状態の説明＋次アクション導線」を持つ。要件の追加なし（§2-3表の転記のみ）。

## 仕様（§2-3 表）

| # | 画面・状態 | 現状 | あるべき表示 |
|---|-----------|------|-------------|
| 1 | コレクション/戦績タブ・バトル0回 | 数値0の羅列 | 「まだバトルしていません」＋バトルへ導線 |
| 2 | コレクション/履歴・0件 | 空リスト | 「バトルすると直近20件が残ります」＋バトルへ導線 |
| 3 | コレクション/実績・全未達成 | 進捗0%の羅列 | 最初に達成しやすい実績を上部にハイライト |
| 4 | ローカルリーグ・週初回 | 順位表のみ | 「今週のRPを稼いで順位を上げよう」の説明行 |
| 5 | URL入力・不正コード | 汎用エラーテキスト | 失敗理由別のインライン文言（形式不正/チェックサム不一致）。※URL対戦のデコードはローカル完結のためオフライン警告は**出さない** |
| 6 | ゲストプレビュー・デコード失敗 | エラーテキストのみ | 失敗理由別の文言（形式不正/改ざん検知）＋入力画面へ戻る導線 |
| 7 | データ保護・復元失敗 | 汎用エラー | 失敗理由（形式/チェックサム=F5連動）＋「コード全体をコピーし直してください」 |
| 8 | ガチャ結果・覚醒上限補填 | 補填コイン表示（済） | 変更なし（確認のみ） |
| 9 | `?battle=` 直リンク・デコード失敗 | `main.dart` が `debugPrint` のみで通常起動 | 生の `battle` パラメータを保持して起動し、同意ゲート通過後にデコード。失敗時は理由別エラー（形式不正/改ざん検知）＋URL入力画面への導線を表示 |

共通コンポーネント: `lib/presentation/widgets/empty_state_card.dart`（アイコン＋状態説明＋次アクションボタン）。画面設計は §3-4。

## 新規ファイル
- `lib/presentation/widgets/empty_state_card.dart`
- `lib/presentation/decode_failure_copy.dart`（形式不正/改ざん検知/チェックサムの文言）
- `lib/presentation/battle_deep_link.dart`（同意後デコードと失敗時導線）

## テスト基準
- [ ] EmptyStateCard: 文言・導線ボタンのタップでコールバックが走る
- [ ] §2-3 #1/#2/#3/#4/#5/#6/#7/#9 の表示が仕様どおり
- [ ] #8 はガチャ結果の補填表示を変更しない

## 完了条件
- [ ] flutter analyze: エラー0
- [ ] flutter test: 全パス

## 制約
- F4 / F5 / F6 は対象外。#7 のチェックサム経路は IntegrityException 表示のみ用意し、バックアップ v2 は実装しない。

---
## Generator ログ
- EmptyStateCard を新設し、§2-3 表の対象画面へ適用。#8 ガチャ結果は補填表示を確認し変更なし。
- `?battle=` は生パラメータ保持のまま同意後デコードし、失敗時はゲストプレビュー上に理由別エラー＋入力画面導線。
- #7 チェックサム文言は IntegrityException 経路のみ（F5 未実装）。

---
## 評価
（Evaluator が検証結果を追記）
